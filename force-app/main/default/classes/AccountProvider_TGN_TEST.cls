/* Generated by TestGen on Mon Sep 25 2023 at 9:51 AM (GMT) */
@isTest
private class AccountProvider_TGN_TEST{
    @isTest
    private static void createNewAccount_TGN_TEST(){
        Test.startTest();
        AccountProvider.createNewAccount(new Account());
        Test.stopTest();
    }
    @isTest
    private static void searchAccountRecords_TGN_TEST(){
        Database.SaveResult dsr;
        Account drAccount;
        drAccount=new Account();
        dsr=Database.insert(drAccount,false);
        Test.startTest();
        AccountProvider.searchAccountRecords(new Account());
        AccountProvider.searchAccountRecords(null);
        Test.stopTest();
    }
    @isTest
    private static void searchAccountType_TGN_TEST(){
        Database.SaveResult dsr;
        Account drAccount;
        drAccount=new Account(Type='a');
        dsr=Database.insert(drAccount,false);
        Test.startTest();
        AccountProvider.searchAccountType(new Account());
        Test.stopTest();
    }
    @isTest
    private static void searchAccountSLAandType_TGN_TEST(){
        Database.SaveResult dsr;
        Account drAccount;
        drAccount=new Account(Type='a',SLA__c='a');
        dsr=Database.insert(drAccount,false);
        Test.startTest();
        AccountProvider.searchAccountSLAandType(new Account());
        Test.stopTest();
    }
    @isTest
    private static void searchAccountRecordByAlphabet_TGN_TEST(){
        Database.SaveResult dsr;
        Account drAccount;
        drAccount=new Account();
        dsr=Database.insert(drAccount,false);
        Test.startTest();
        AccountProvider.searchAccountRecordByAlphabet('a');
        AccountProvider.searchAccountRecordByAlphabet('');
        Test.stopTest();
    }
    @isTest
    private static void searchAccounts_TGN_TEST(){
        Database.SaveResult dsr;
        Account drAccount;
        drAccount=new Account();
        dsr=Database.insert(drAccount,false);
        Test.startTest();
        AccountProvider.searchAccounts('a');
        AccountProvider.searchAccounts('');
        Test.stopTest();
    }
    @isTest
    private static void getAccountRecords_TGN_TEST(){
        Database.SaveResult dsr;
        Account drAccount;
        drAccount=new Account();
        dsr=Database.insert(drAccount,false);
        Test.startTest();
        AccountProvider.getAccountRecords();
        Test.stopTest();
    }
    @isTest
    private static void getAccWithCon_TGN_TEST(){
        Database.SaveResult dsr;
        Account drAccount;
        drAccount=new Account();
        dsr=Database.insert(drAccount,false);
        Test.startTest();
        AccountProvider.getAccWithCon();
        Test.stopTest();
    }
    @isTest
    private static void getAccounts_TGN_TEST(){
        Database.SaveResult dsr;
        Account drAccount;
        drAccount=new Account();
        dsr=Database.insert(drAccount,false);
        Test.startTest();
        AccountProvider.getAccounts();
        Test.stopTest();
    }
    @isTest
    private static void AccountProvider_TGN_TEST(){
        AccountProvider obj = new AccountProvider();
    }
}